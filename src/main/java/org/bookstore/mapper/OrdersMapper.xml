<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.bookstore.mapper.OrdersMapper">
    <insert id="saveOrder" parameterType="org.bookstore.po.Orders">
  	insert into orders(userid,orderdate) values(#{user.id},#{orderdate})
  </insert>
</mapper>